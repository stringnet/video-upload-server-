{
  "name": "video-upload-server",
  "version": "1.0.0",
  "description": "Servidor para subir videos para ScanMe.io",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "video",
    "upload",
    "express",
    "scanme"
  ],
  "author": "TuNombre",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",  // Ejemplo de versión
    "multer": "^1.4.5-lts.1" // Ejemplo de versión
  }
}
